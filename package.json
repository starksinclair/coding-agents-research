{
  "name": "coding-agents-research",
  "version": "1.0.0",
  "private": true,
  "description": "Test harness for evaluating coding-agent fixes against the Project Sin codebase.",
  "scripts": {
    "test": "jest",
    "test:rw": "jest tasks/real-world"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jsdom": "^24.1.0"
  }
}
